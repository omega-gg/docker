#--------------------------------------------------------------------------------------------------
# vlc-android
#--------------------------------------------------------------------------------------------------

jobs:
- job: vlc_android
  pool:
    vmImage: "ubuntu-latest"
  steps:
  - bash: |
      docker build -t image vlc-android
      docker save image > vlc-android.tar
  - publish: $(System.DefaultWorkingDirectory)/vlc-android.tar
    artifact: vlc-android
